version: '3.8'
services:
  steam-api:
    image: stopfmich/steam-api-php:latest-production
    ports:
      - "8000:80"
    environment:
      - STEAM_API_KEY=${STEAM_API_KEY}
      - APP_DEBUG=false
      - LOG_LEVEL=info
    volumes:
      - ./logs:/var/www/html/logs